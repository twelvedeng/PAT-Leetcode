#include <iostream>
#include <map>
using namespace std;
struct gra{
	int gp = -1, gm = -1, gf = -1, g; 
};
map<string, stu> stu;
int main() {
	int p, m, n;
	cin >> p >> m >> n;
	for(int i = 0; i < p; i++)
		cin >> 
}
